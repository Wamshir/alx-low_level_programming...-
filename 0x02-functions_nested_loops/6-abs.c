#include "main.h"                        /**
 * 6-abs.c - Absolute Value
 * t - integer to be computed
 * Return: Absolute Value
 */
int _abs(int t)
{
        if (t >= 0)
                return (t);
        else
                return (-t);
}
